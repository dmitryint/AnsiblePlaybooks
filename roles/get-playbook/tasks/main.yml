---
- name: get the latest ansible playbook 
  git: repo={{ git_url }} dest=/etc/ansible/AnsiblePlaybooks update=yes clone=yes

- name: create the azure directory 
  file: path={{ ansible.env.HOME }}/.azure state=directory

- name: copy the azure crediantials files 
  template: src=credentials.j2 dest={{ ansible.env.HOME }}/.azure/credentials
